apiVersion: v1
kind: Service
metadata:
  name: {{ include "sampleserver.fullname" . }}
  labels:
    {{- include "sampleserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sampleserver.selectorLabels" . | nindent 4 }}
